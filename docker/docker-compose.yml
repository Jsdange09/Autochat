# docker/docker-compose.yml
# Combined docker-compose for Autochat (your friend) + Aegis MLOps layer (you)
# Run: docker compose up -d

version: "3.9"

services:

  # ── Your friend's FastAPI backend (with your MLOps layer injected) ──
  backend:
    build:
      context: ..
      dockerfile: docker/Dockerfile.backend
    ports:
      - "8000:8000"
    volumes:
      - ../backend/chroma_db:/app/backend/chroma_db
      - ../backend/logs:/app/backend/logs
      - ../data:/app/data
    environment:
      - CONFIDENCE_THRESHOLD=0.65
      # 🔑 REPLACE WITH YOUR REAL KEYS:
      - ARIZE_API_KEY=ak-d2881775-a106-4e6b-a2f7-952df67e846d-16HwDiHVbGXC3p8GfOUSoZbEe9yReG2C
      - ARIZE_SPACE_ID=U3BhY2U6NDM4NDM6Yy9NVg==
      - WANDB_API_KEY=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
      - WANDB_PROJECT=aegis-autochat
    depends_on:
      - prefect
    networks:
      - aegis-net

  # ── Your friend's React frontend ──
  frontend:
    build:
      context: ../frontend/chatbot-ui
      dockerfile: Dockerfile
    ports:
      - "5173:5173"
    networks:
      - aegis-net

  # ── Prefect orchestration server ──
  prefect:
    image: prefecthq/prefect:3-latest
    command: prefect server start --host 0.0.0.0
    ports:
      - "4200:4200"
    networks:
      - aegis-net

  # ── Prometheus (scrapes /metrics from your friend's API) ──
  prometheus:
    image: prom/prometheus:latest
    ports:
      - "9090:9090"
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml
    networks:
      - aegis-net

  # ── Grafana (your friend's dashboard; admin/admin) ──
  grafana:
    image: grafana/grafana:latest
    ports:
      - "3000:3000"
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=admin
    depends_on:
      - prometheus
    networks:
      - aegis-net

networks:
  aegis-net:
    driver: bridge