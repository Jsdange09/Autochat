# requirements.txt
# ── Your friend's original packages ──
fastapi
uvicorn[standard]
pydantic
llmware
chromadb
sentence-transformers
prometheus_client
prefect>=3.0.0
httpx

# ── Your MLOps additions ──
ray[default]>=2.20.0
PyMuPDF           # fitz — PDF reading for Ray pipeline
python-dotenv     # .env file loading

# Arize AI observability
# 🔑 Needs ARIZE_API_KEY + ARIZE_SPACE_ID in .env
arize-sdk

# Weights & Biases experiment tracking
# 🔑 Needs WANDB_API_KEY in .env
wandb

# Pulumi infrastructure (run separately from infra/ folder)
# pulumi
# pulumi-docker

# Optional: for real Delta Lake (requires Java 11+)
# delta-spark
# pyspark