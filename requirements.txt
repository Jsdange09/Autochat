# ── Friend's original packages ──
fastapi
uvicorn[standard]
pydantic
llmware
chromadb
sentence-transformers
prometheus_client
prefect>=3.0.0
httpx

# ── Your MLOps additions ──
ray[default]>=2.20.0
PyMuPDF
# [FIX] python-dotenv was listed but NOT imported in api.py — now fixed
python-dotenv>=1.0.0

# Arize AI — PINNED to 7.x (v8 broke the logging API we use)
# [FIX] arize==8.x removed arize.api.Client entirely
arize>=7.0.0,<8.0.0

# Weights & Biases
wandb>=0.17.0